function op(num1, num2) {
    soma = num1 + num2
    subtracao = num1 - num2
    multiplicacao = num1 * num2
    divisao = num1 / num2
    console.log(soma, subtracao, multiplicacao, divisao)
}

op(4, 2)
